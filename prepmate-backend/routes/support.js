const express = require("express");
const router = express.Router();

// Placeholder routes - to be implemented
router.get("/", (req, res) => {
  res.json({ message: "Support routes - to be implemented" });
});

module.exports = router;
